# Copy this file to .env for local dev only (optional).
# .env is git-ignored and is NOT bundled into the built .exe, so this only
# affects `npm start` during development.

# Public URL of your deployed auth backend (Cloudflare Worker). Not a
# secret. If unset, the app falls back to the URL hardcoded in src/main.js.
BACKEND_URL=

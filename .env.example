# Copy this file to .env for local dev defaults (optional).
# These only pre-fill Settings on first launch — the app also lets you
# edit and save these values from the UI at runtime.

API_BASE_URL=
API_ENDPOINT=
API_KEY=
